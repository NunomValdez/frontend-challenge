{
  "files": [],
  "references": [
    {
      "path": "./tsconfig.node.json"
    },
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.vitest.json"
    }
  ],

  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "moduleResolution": "node",
    "allowJs": true,
    "baseUrl": ".", // This must be set if "paths" is set.
    "paths": {
      "@/*": ["src/*"] // Assuming that "@" should point to your "src" folder.
    },
  }
}
